<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearnTracker"
        tools:targetApi="31">
        <activity
            android:name=".pre.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".pre.PrivacyActivity"/>
        <activity android:name=".pre.SelectActivity" />
        <activity android:name=".pre.WorkingTitleActivity" />

        <activity android:name=".pages.MainActivity"/>
        <activity android:name=".pages.TrackTimeActivity" />
        <activity android:name=".pages.ScheduleActivity" />
        <activity android:name=".pages.CalendarActivity" />
        <activity android:name=".pages.SubjectsActivity" />
        <activity android:name=".pages.DashboardActivity" />
        <activity android:name=".pages.TasksActivity" />
        <activity android:name=".pages.TodoPage" />

        <service
            android:name=".service.TimerService"
            android:foregroundServiceType="specialUse"
            tools:ignore="ForegroundServicePermission" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>